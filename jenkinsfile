pipeline {
    agent { 
	node { label 'MVN3' }
        }
    stages {
        stage('vcs') {
            steps {
             git url: 'https://github.com/nagarjunaduggireddy/openmrs-core.git', 
             branch: 'master'
            }
        }
        stage('build') {
            steps {
                sh "mvn package"
            }
        }
    }
}
