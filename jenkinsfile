pipeline {
    agent { node { label 'MVN3' } }
	cron { cron('H 18 * * 1-5') }
    stages {
        stage('vcs') {
            steps {
             git url: 'https://github.com/nagarjunaduggireddy/openmrs-core.git', branch: 'master'
            }
        }
        stage('build') {
            steps {
                sh "mvn package"
            }
        }
    }
}
